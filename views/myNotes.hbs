<main class="container">
    <div class="container px-4 py-5" id="hanging-icons">
        <h1 class="pb-4 border-bottom text-center">My Notes</h1>
        {{#each notes}}
        <div class="navbar navbar-expand-lg bg-body-tertiary rounded">
            <div class="container-fluid">
                <div class="collapse navbar-collapse d-lg-flex">
                    <div class="d-lg-flex col-lg-1">
                        <span class="{{#ifeq status "public"}} badge bg-success-subtle border border-success-subtle
                            text-success-emphasis {{else}} badge bg-danger-subtle border border-danger-subtle
                            text-danger-emphasis {{/ifeq}}">
                            {{#ifeq status "public"}} Public {{else}} Private {{/ifeq}}
                        </span>
                    </div>
                    <div class="navbar-brand col-lg-3 me-0">{{substr "30" title}}</div>
                    <div class="d-lg-flex col-lg-7">
                        <p class="text-left" style="margin-block-end: 0 !important;">{{substr "80" body}}</p>
                    </div>
                    <div class="d-lg-flex col-lg-1 justify-content-lg-end">
                        <button class="btn btn-warning">Edit Note</button>
                    </div>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</main>